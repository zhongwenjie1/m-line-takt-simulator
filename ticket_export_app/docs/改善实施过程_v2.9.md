# M-Line 混流节拍仿真系统 改善实施过程 v2.9

> 本文档按轮次记录实际修改、验证结果和用户确认状态。  
> 实施原则：一轮只做一个任务，上一轮确认后才进入下一轮。

## 第0轮：归档方案并建立真实数据回归基线

### 任务状态

- 状态：第0轮执行完成，等待用户确认后提交并进入第1轮
- 是否修改业务逻辑：否
- 是否提交：否
- 是否打包：否

### 修改前仓库状态

- 正式仓库：`/Users/zhongwenjie/clean_repos/m-line-takt-simulator`
- 分支：`main`
- HEAD：`54776ab`
- 业务源码 diff：无
- 既有未跟踪资料：调查资料、整合调查报告、改善计划方案

### 本轮目标

1. 固定按比例1240台和按数量1148台两组真实回归输入。
2. 建立可重复运行的只读基线脚本。
3. 记录修改前关键结果，为后续每轮生成差异表。
4. 不修改排程、分析、UI、导出和打包逻辑。

### 实际新增文件

1. `ticket_export_app/scripts/verify_v29_real_data_baseline.py`
2. `ticket_export_app/docs/改善实施过程_v2.9.md`
3. `ticket_export_app/reports/v2.9_round0_baseline.json`

### 回归脚本检查内容

1. 车辆生成数量和车型数量。
2. 投车序列前30台、第一台A的位置和最大连续台数。
3. 分析窗口内下线车辆、达标车辆、首末下线时间和整体节拍。
4. 累计原始完工后等待及主要归因工位。
5. 完整批次最后下线时间。
6. 相邻下线间隔累计及超过目标节拍次数。
7. 正加工资源的加工重叠、资源容量超限和顺序倒置。
8. 全部车辆在同一工位同一线别的物理占位重叠，包括加工车与0工时等待车同槽。
9. 最后一台车的两条完成时间复算链。
10. 调查资料存在时，逐车核对车型、实际投车时间、下线完成时间、等待工时和单车贯通时间。

### 修改前真实数据基线

#### 按比例投车

- A:B:C：2:3:5
- 生成车辆：1240台
- 分析窗口内下线：1046台
- 达标车辆：210台
- 第一台下线完成时间：861.0秒
- 最后一台窗口内下线完成时间：68987.5秒
- 整体节拍：65.1928229665秒/台
- 窗口内累计原始完工后等待：243807.0秒
- 全部车辆最后下线完成时间：81543.5秒
- 全部车辆累计原始完工后等待：285937.0秒
- 相邻下线间隔超过58秒：469次

#### 按数量投车

- A/B/C：276/306/566
- 生成车辆：1148台
- 分析窗口内下线：1036台
- 达标车辆：239台
- 第一台下线完成时间：1423.5秒
- 最后一台窗口内下线完成时间：68952.5秒
- 整体节拍：65.2454106280秒/台
- 窗口内累计原始完工后等待：235263.5秒
- 全部车辆最后下线完成时间：75885.5秒
- 全部车辆累计原始完工后等待：260263.5秒
- 相邻下线间隔超过58秒：570次
- 第一台A：第186位
- 最大连续同车型：5台

### 当前已知风险基线

1. 正加工资源的加工重叠和容量超限均为0；这只证明加工设备堆没有超配，不代表工位物理车位正确。
2. 按比例场景同工位同线物理占位重叠1357组，最大同时在位2台。
3. 按数量场景同工位同线物理占位重叠1624组，最大同时在位3台。
4. “0工时等待车未离开、后一台已开始加工”分别为986组和1086组，涉及615台和565台后一加工车辆。
5. 当前按车号排程，同一正加工资源未出现车号倒序服务；共用工位是否应保持车号顺序仍需现场规则确认。
6. 当前尚未实现最终业务口径的“岗位能力瓶颈等待”，脚本明确标记为未实现，避免与原始等待混用。

### 运行命令

```bash
/Users/zhongwenjie/程序/.venv312/bin/python \
  ticket_export_app/scripts/verify_v29_real_data_baseline.py \
  --evidence ticket_export_app/调查资料.txt
```

确定性检查：连续运行两次，将标准输出分别保存到临时目录并比较文件哈希和内容。

### 自动验证结果

1. `py_compile`：通过。
2. 两次完整回归输出内容完全一致。
3. 增加逐工位证据核对、加工资源容量检查和完整同槽检查后的当前基线输出 SHA-256：`da6f4064e2ca6d3a90601d33f7fd6c997b10281195d173286bfa9dded5e4823f`。
4. 基线常量断言：通过。
5. 调查资料逐车核对：
   - 按比例1240台，差异0；
   - 按数量1148台，差异0。
   - 两组合计2388台、35820条工位过程记录，车型、投车、下线、等待、贯通时间、工位、开始、加工、投入等待和完工后等待均无差异。
6. 正加工资源检查：
   - 加工重叠0；
   - 加工能力超限0；
   - 同资源车号倒序0。
7. 完整工位物理车位检查：
   - 按比例场景1357组同槽重叠，最大同时在位2台；
   - 按数量场景1624组同槽重叠，最大同时在位3台；
   - 按比例986组、按数量1086组属于0工时等待车未离开时后一台开始加工；
   - 按数量场景另有2组0工时车辆彼此同槽。

8. 截图1340秒专项核对补充：
   - ARHUD+慢充2号线同时存在 Car#12（A，0工时等待）和 Car#14（B，正在加工）；
   - Car#12 停留区间为1181.5～1342.5秒；
   - Car#14 在1312.0秒进入ARHUD+慢充，因此两车在1312.0～1342.5秒同槽；
   - Car#14提前离开上游ADAS后，Car#16在1325.5秒开始ADAS加工；
   - 若ARHUD+慢充2号线车位容量为1，Car#14最早应在1342.5秒进入，Car#16最早也只能在1342.5秒进入ADAS；
   - 当前Car#16因此至少提前17.0秒开始加工。
9. 工位卡片和底部摘要的“还有”不是同一数据：
   - ARHUD+慢充2号线卡片内橙色“还有1台”表示该槽位还有Car#12未展开，是本次物理占位问题的直接证据；
   - 底部“加工中车辆”中C-16后面的“还有2台”是全线加工列表的折叠结果，不代表ADAS同线还有2台；
   - 1340秒核心数据中实际有17台车辆正在加工，底部仅显示2台，正确折叠数量应为15台；当前代码先生成一次“还有14台”，随后又二次压缩成“还有2台”，属于独立UI计数问题。

### 第0轮发现的证据修正

原计划把按比例场景中的 Car#11 / Car#12 作为“同一ST11车位同时停两台”的验收证据。基线脚本按“工位 + 线别”重新核对后，这两台车不能证明同一线别车位重叠。

后续根据用户截图继续核对发现，基线脚本此前只统计“0工时等待车辆之间的重叠”，遗漏了“0工时等待车辆与正在加工车辆同槽”的物理占位冲突。1340秒截图中的 Car#12 与 Car#14 正是这种情况，并且已经向上游传导，造成 Car#16 提前开始ADAS加工。

按数量场景确实存在0工时节点同工位同线最大2台的记录。第1轮R2实施前，需要以脚本输出的真实车辆、工位、线别和时间作为修复证据。

当前脚本定位到的真实记录为：Car#366与Car#372，在“气囊安装+绝缘”2号线，于27504.5秒同时在位。

这一发现不修改业务逻辑，只补齐基线检查和R2验收证据。第1轮必须同时检查“零工时车彼此重叠”和“零工时等待车与加工车同槽”，不能只检查其中一种。

后续扩大到两组完整真实数据后确认：按比例场景共有1357组同槽占位重叠，其中986组为零工时等待车尚未离开时后一台开始加工，涉及615台后一加工车辆；按数量场景共有1624组，其中1086组为该类提前进入，涉及565台后一加工车辆。加工资源本身的并行能力超限为0，说明风险集中在“工位物理车位没有限制”，而不是设备资源堆同时加工两台车。完整说明见《真实数据可信度复核_提前加工与同槽占位.md》。

按全部车辆在同一工位同一线别的实际在位区间统计：

- 按比例场景最大同时在位2台；
- 按数量场景最大同时在位3台；
- 这些数据作为R2修改前的正式容量基线。

### Diff范围

- 本轮新增回归脚本和实施过程文档；
- 本轮生成固定基线JSON；
- 改善计划方案和整合调查报告为既有未跟踪文档；
- `core`、`ui`、`main.py`、workflow和打包配置无修改。

### 本轮最终验收

1. 相同代码连续运行两次，输出逐字节一致：通过。
2. 两次输出SHA-256均为 `da6f4064e2ca6d3a90601d33f7fd6c997b10281195d173286bfa9dded5e4823f`：通过。
3. 调查资料与当前程序逐车逐工位差异为0：通过。
4. 基线能定位具体车辆、工位、线别和重叠时间：通过。
5. 正加工容量超限为0，同时明确保留1357组/1624组物理同槽风险：通过。
6. 业务源码、UI、workflow和打包配置未修改：通过。

### 后续轮次顺序复核

最新风险等级和依赖关系使第2轮、第3轮顺序发生调整：

- 原顺序：R2 → R10 → R9；
- 当前顺序：R2 → R9 → R10。

R2仍为第1轮。R9为已确认影响排程结果的P0风险，提前至第2轮。R10依赖现场汇流规则确认，保留为第3轮独立任务。第4轮及之后暂不变化。

### 用户确认

用户已确认R2结果可提交。

### 提交与回退点

- 第1轮提交：`e7c0e60 fix physical station slot capacity`。
- 第2轮R9以该提交作为修改前基线。

## 第2轮前置确认：R9确定性平准化与排列冻结

> 历史说明：本节记录第三轮模拟定版前的纯平准预演，`8c79e32b…` 已被后文第2轮正式实施的“靠上限投车 + 落后容差 τ=1.0”及 `dac6086f…` 替代。

### 任务状态

- 状态：模拟排序和方案文档完成，等待用户确认序列
- 是否修改正式投车算法：否
- 是否修改UI：否
- 是否提交：否

### 用户定版方向

1. 按比例/数量构成使用确定性平准化规则，不使用随机排序。
2. 新增 `排列冻结` 功能按钮。
3. 平准序列经用户确认并冻结后，后续分析、动画、日志和导出复用同一完整序列。
4. 冻结保证排列不被每次分析重新生成，不保证岗位矩阵、目标节拍等模型参数变化后分析数值不变。

### A276/B306/C566模拟结果

- 总台数：1148台；
- 前30台：`CBACCBACCBACBCACBCACBCACBCACBC`；
- 前100台：A24/B27/C49；
- 前300台：A72/B80/C148；
- 前600台：A144/B160/C296；
- 全量：A276/B306/C566；
- 实际最大连续同车型：2台；
- 任意前缀各车型累计数量偏差均小于1台；
- 完整序列SHA-256：`8c79e32bb5e993f5f0833e985946aec894e5b1654bf74bce908aee6d420ca90e`。

完整序列和规则说明见：`ticket_export_app/docs/车辆数据模拟排序_R9确认版.md`。

### R9正式实施边界

1. 只修改确定性序列生成、冻结状态和冻结序列传递。
2. 不修改R2物理车位容量规则。
3. 不修改等待、风险提示和达标口径。
4. 任何分析结果变化必须能追溯到车辆序列变化。
5. 用户确认模拟序列后才进入正式代码修改。

### 提交与回退点

- 第0轮已提交：`f7e1fc8 archive v2.9 investigation baseline`。
- 该提交是后续排程修改的回退基点。

## 第1轮：R2 0工时节点有限容量

### 任务状态

- 状态：代码修改和自动验证完成，等待用户确认
- 是否修改业务逻辑：是，仅修改排程物理车位约束
- 是否提交：否
- 是否打包：否

### 修改前问题

1. 0工时车辆保留工位过程记录，但不登记工位物理车位。
2. 当前工位离开时直接查看下一个正工时资源，跳过中间0工时节点的逐格容量。
3. 按比例真实场景存在1357组同槽重叠，按数量场景存在1624组。
4. 截图链路中Car#12尚在ARHUD+慢充2号线，Car#14已经进入同槽，进而使Car#16至少提前17秒开始ADAS加工。

### 本轮修改范围

业务代码：

- `ticket_export_app/core/tickets.py`

只读验证与结果：

- `ticket_export_app/scripts/verify_v29_round1_r2.py`
- `ticket_export_app/reports/v2.9_round1_r2_result.json`
- 本实施过程文档

未修改UI、分析口径、投车序列、GitHub Actions、打包配置和导出逻辑。

### 实现说明

1. 加工设备资源和工位物理车位分开管理。
2. 每个工位、每条实际流转线固定只有1个物理车位。
3. 正工时车辆同时占加工资源和物理车位。
4. 0工时车辆保留过程记录，只占物理车位，不占加工设备。
5. 0工时节点优先保持车辆原线别，不因该节点自身的线别配置强制换线。
6. 车辆只有在紧邻下一个流转节点可接收时才离开当前节点，车位满时等待逐级向上游回传。
7. 负工时保护、车型序列、设备能力、分析与显示逻辑保持不变。

### 最小回堵场景

场景：A工位30秒 → 0工时节点 → B瓶颈100秒，单线单车位，连续4台A车。

- 修改前Car#4在A工位90秒开始；
- 修改后Car#4在A工位130秒开始；
- 同槽重叠：0；
- 单车时间链重叠、断档、完成时间和等待加法错误：均为0。

### 截图链路复核

修改后按比例真实场景：

| 车辆 | 工位 | 线别 | 开始 | 加工完成 | 离开 | 说明 |
|---|---|---|---:|---:|---:|---|
| Car#12 | ARHUD+慢充 | 2号线 | 1228.0 | 1228.0 | 1342.5 | 0工时，占物理车位等待下游 |
| Car#14 | ARHUD+慢充 | 2号线 | 1342.5 | 1394.0 | 1458.5 | 在Car#12离开后进入，不再同槽 |
| Car#16 | ADAS | 2号线 | 1348.5 | 1455.5 | 1458.5 | 原开始1325.5，修复后不再提前 |

Car#12与Car#14交接时间严格相等，没有重叠。Car#16开始时间比原结果推迟23秒，并晚于原调查要求的最早1342.5秒。

### 两组真实数据回归

#### 按比例场景

| 指标 | 第0轮基线 | R2修改后 | 差异 |
|---|---:|---:|---:|
| 窗口内下线 | 1046 | 1024 | -22 |
| 达标车辆 | 210 | 206 | -4 |
| 第一台下线 | 861.0 | 861.0 | 0 |
| 窗口末台下线 | 68987.5 | 68996.5 | +9.0 |
| 整体节拍 | 65.1928 | 66.6036 | +1.4108 |
| 窗口内原始完工后等待 | 243807.0 | 217628.0 | -26179.0 |
| 全部车辆最后下线 | 81543.5 | 83308.0 | +1764.5 |
| 同槽重叠 | 1357 | 0 | -1357 |
| 最大同时在位 | 2 | 1 | -1 |

#### 按数量场景

| 指标 | 第0轮基线 | R2修改后 | 差异 |
|---|---:|---:|---:|
| 窗口内下线 | 1036 | 1032 | -4 |
| 达标车辆 | 239 | 238 | -1 |
| 第一台下线 | 1423.5 | 1423.5 | 0 |
| 窗口末台下线 | 68952.5 | 68939.0 | -13.5 |
| 整体节拍 | 65.2454 | 65.4855 | +0.2400 |
| 窗口内原始完工后等待 | 235263.5 | 227878.5 | -7385.0 |
| 全部车辆最后下线 | 75885.5 | 76131.5 | +246.0 |
| 同槽重叠 | 1624 | 0 | -1624 |
| 最大同时在位 | 3 | 1 | -2 |

### 其他自动检查

1. 两组仍分别保留18600条和17220条工位过程记录，0工时节点没有消失。
2. 正加工资源能力超限：0。
3. 单车相邻工位时间链重叠：0。
4. 单车相邻工位时间链断档：0。
5. `开始+加工=加工完成`错误：0。
6. `离开-加工完成=完工后等待`错误：0。
7. ST2单设备、A工时0的定向场景中，A车继续使用1号线和2号线，未被ST2的1号线配置全部强制到1号线；B车仍只在ST2的1号线加工。
8. R2验证连续运行两次输出一致，SHA-256为 `c871ff8e233ddce5182ac2cdcd85eb4a1d896eddb0bbe9ed2ec61b4c407fe2f0`。
9. `py_compile`：通过。

### 结果解释边界

1. 下线数、整体节拍和完成时间变化是物理车位回堵带来的排程结果变化，不要求保持第0轮旧值。
2. 原始完工后等待是多车多工位累加值；它下降而总完成时间上升并不构成算术矛盾，也不能直接用于判断真实瓶颈损失。
3. 岗位能力瓶颈等待尚未在本轮实现，继续由后续独立轮次处理。
4. 比例投车仍为`AABBBCCCCC`，R9尚未修改。
5. 共用资源仍按现有车号顺序服务，R10现场汇流规则尚未确认。

### Diff范围

- 本轮业务修改仅在 `ticket_export_app/core/tickets.py`。
- 新增R2只读验证脚本和结果JSON。
- 更新本实施过程文档。
- `ui`、`analysis.py`、`schedule_v2.py`、`input_parser.py`、`main.py`、workflow和packaging均无修改。

### 用户确认

等待确认。

## 第2轮：R9 靠上限投车 + 落后容差 + 排列冻结

### 任务状态

- 状态：代码修改和自动验证完成，等待用户功能确认
- 最终规则：第三轮模拟“靠上限投车 + 落后容差 τ=1.0”
- 修改文件：`ticket_export_app/core/tickets.py`、`ticket_export_app/ui/export_ticket_window.py`
- 按比例投车：本轮保持原固定比例块逻辑
- 提交：未提交

### 实现内容

1. `tickets.build_vehicle_sequence()` 成为排程和冻结预览共用的唯一序列生成入口。
2. 按数量交替混流使用规则版本 `quantity-balanced-run-tau1-v1`。
3. 当前车型在未达到最大连续上限、且其他车型累计落后未达到1台时继续投放。
4. 触发落后阈值或连续上限后，按欠投量、剩余数量、A→B→C依次决策。
5. `schedule()` 和 `schedule_and_export()` 支持显式传入完整冻结序列；传入后不再重新排序。
6. 运行参数区新增 `排列冻结` 按钮及“未冻结/已冻结”状态。
7. 冻结保存完整车型列表、规则版本、输入签名、生成时间和SHA-256；分析、动画、车辆日志和导出共用该列表。
8. 投车模式、A/B/C数量、总台数、排列方式、最大连续台数或规则版本变化时冻结失效。
9. 岗位矩阵、目标节拍和分析时间不改变冻结车辆顺序，但允许排程结果随模型参数变化。
10. 按数量交替混流未冻结时，分析和导出明确提示先冻结，不静默重新生成排列。

### A276/B306/C566确认结果

| 项目 | 结果 |
|---|---|
| 前30台 | `CCCBAAABCCCBBBCCAACCCCBBAACCCC` |
| 前100台 | A24 / B26 / C50 |
| 前300台 | A72 / B80 / C148 |
| 前600台 | A144 / B159 / C297 |
| 全量 | A276 / B306 / C566 |
| 实际最大连续 | 5台 |
| SHA-256 | `dac6086f3b896255eeb77cb82679b0334e49ab972a5e9ed6aa0cd4e6d347f08f` |
| 重复生成 | 两次完整序列一致 |

### 真实数据回归

按数量模式没有“分析时间”输入，正式程序按完整批次1148台统计。本轮与第1轮R2结果对比使用相同工位矩阵、目标节拍和数量，只改变R9车辆排列。

| 指标 | R2旧排列 | R9最终排列 | 差异 |
|---|---:|---:|---:|
| 投车车辆 | 1148 | 1148 | 0 |
| 全批次下线车辆 | 1148 | 1148 | 0 |
| 第一台下线 | 1423.5 | 1423.5 | 0 |
| 最后一台下线/总完成时间 | 76131.5 | 76152.5 | +21.0 |
| 全批次OUT平均间隔 | 65.1334 | 65.1517 | +0.0183 |
| 全批次原始完工后等待合计 | 253105.5 | 253952.0 | +846.5 |

调查脚本曾额外把按数量结果截到69000秒，得到1032/1038台。该数字仅用于与历史1150分钟调查资料交叉对照，不是按数量模式的正式“下线车辆”指标，也不作为R9验收结论。

按比例场景仍为原固定比例块，R9没有修改其排列。按比例模式继续使用用户填写的分析时间窗口。

### 回归断言

1. 两组真实场景同槽重叠均为0，最大同时在位为1。
2. 正加工资源能力超限为0。
3. 单车相邻工位时间链重叠、断档均为0。
4. `开始+加工=加工完成`和`离开-加工完成=完工后等待`错误均为0。
5. `py_compile`通过。
6. UI offscreen验证：冻结成功、状态显示哈希前缀、连续读取同一序列一致；A数量变化后冻结立即失效。

### 本轮边界

1. 未修改R2物理车位容量和0工时节点逻辑。
2. 未修改等待、风险提示、达标率、整体节拍等计算口径。
3. 未修改按比例投车排列。
4. 未修改GitHub Actions、打包配置和版本号。
5. 当前等待用户功能确认，不自动进入第3轮R10。
6. 当前冻结保存在本机当前会话；跨人员复现所需的冻结导出/导入登记为后续独立设计，本轮不实现。
7. 后续冻结文件边界：完整车型序列、A/B/C数量、最大连续台数、规则版本、τ值、生成时间和SHA-256；导入必须逐项校验，避免不同人员生成不同排列后误认为同一分析输入。

## 第3轮前置确认：R10 共用资源服务顺序

### 当前状态

- R9提交：`b326673 implement deterministic quantity sequence freeze`
- 本节性质：R10修改前的只读证据复核
- 是否修改排程：否
- 待确认业务规则：共用/单设备工位按“实际先到先服务”还是“严格保持原车序”

### R9最终排列后的重新扫描

到达时刻按“进入小跑道前，最后一个正工时工位的加工完成时刻”计算；服务顺序按小跑道实际开始时刻排序。若后一服务车辆的上游完成时刻早于前一服务车辆，则记为一组服务顺序差异。

| 场景 | 相邻服务顺序差异 | 后车最大提前到达 |
|---|---:|---:|
| 按比例（原固定比例块） | 123组 | 331.0秒 |
| 按数量（R9最终冻结排列） | 202组 | 375.5秒 |

旧调查中的184组/293组来自R2、R9实施前的排程结果。车辆排列和物理车位容量变化后，该数字已经失效，R10只能使用本节的新基线继续判断。

按数量最大差异示例：Car#49（C）上游完成4529.0秒、小跑道4529.0秒开始；Car#50（A）上游已在4153.5秒完成，但因当前整车按车号排程，到4579.0秒才进入小跑道，较前车早到375.5秒却后服务。

### 修改前停止条件

1. 如果现场规则是“实际先到先服务”，R10需要修改资源服务顺序，并重新计算全部车辆时间链。
2. 如果现场规则是“严格保持原车序”，当前顺序属于业务约束，R10不修改排程，只记录规则和等待含义。
3. 两种规则会产生不同的开始、等待、下线和总完成时间；未确认现场规则前不能自行选择一种实现。
4. 当前停在业务规则确认点，不修改 `tickets.py`。

### R10现场规则确认

1. 投车端继续使用R9冻结车序和目标节拍，不根据下游车辆的预测完成时间重排投车。
2. 首工位暂时不能接车时，车辆按冻结车序在线外等待，后车不得越过前车进线。
3. 共用/单设备岗位按实际到达时间服务；实际到达时间相同时，按车号小者先服务。
4. 共用岗位需要选择后续线别时，只使用下一节点当前物理车位的实际占用/释放状态；完全同条件时按历史分配数和固定线号破平局。不引入车型优先级或未来完成时间预测。

### R10实现和真实数据复核

- 状态：本地修改和自动验证完成，未提交，等待用户确认。
- 调度方式：普通M-Line模型使用事件队列，车辆完成上游加工时进入下游等待队列，共用/单设备从队列中按实际到达时间取车。
- R2物理车位保留：车辆在下游不能接收时继续占用当前岗位，直到真正转移后才释放当前资源和车位。
- R9冻结排列不变：A276/B306/C566的前30台仍为 `CCCBAAABCCCBBBCCAACCCCBBAACCCC`，SHA-256仍为 `dac6086f3b896255eeb77cb82679b0334e49ab972a5e9ed6aa0cd4e6d347f08f`。

#### A276/B306/C566数量场景

| 项目 | R10结果 |
|---|---:|
| 全批次车辆 | 1148 |
| 69000秒辅助窗口内下线 | 1038 |
| 第一台下线 | 1423.5s |
| 全部车辆最后下线 | 76152.5s |
| 69000秒辅助窗口OUT平均间隔 | 65.1234s |
| 全批次OUT平均间隔 | 65.1517s |
| 全批次原始完工后等待合计 | 239975.5s |
| 首工位进线越车 | 0 |
| 共用/单设备到达顺序违例 | 0 |
| 同资源加工重叠 | 0 |
| 同车位物理重叠 | 0 |

#### 两线平衡和饥饿检查

| 检查项 | 1号线 | 2号线 | 结论 |
|---|---:|---:|---|
| 首个共用岗位分线数 | 574 | 574 | 数量相同 |
| 首工位前平均等待 | 4028.071s | 4035.453s | 差7.382s |
| 首工位前最大等待 | 8111.0s | 8109.0s | 基本相同 |
| 小跑道处理数 | 574 | 574 | 数量相同 |
| 小跑道平均排队 | 27.777s | 27.096s | 差0.681s |
| 小跑道最大排队 | 101.0s | 101.0s | 相同 |

1. 首工位的前20台分线严格为1号线/2号线交替，全程累计分线数最大差为1台。
2. 小跑道服务过程中，两线累计数最大差为3台，同一线最多连续服务3台，未发现一条线长期无法获得服务。
3. 按每100台小跑道服务分窗统计，大多数窗口为50/50，其余为51/49；末段48台为24/24。
4. 每5800秒抽样小跑道排队，队列为0或1台，未持续累积；全过程瞬时最大排队为2台，两线各1台。
5. 首工位外最大等待队列为125台，对应两线的63/62台。该队列是目标投车节拍58s快于当前完整批次实际节拍65.1517s造成的全线产能差，不是单线偏流。
6. 小跑道出现车号次序变化，但抽查均符合实际到达时间。例如Car#5于1517.0s到达、1567.0s开始，Car#4于1547.5s到达、1618.0s开始；车号变序不等于后到车插队。

### R10验证边界

1. 本轮证明当前真实数据下两线未出现永久饥饿，但不把首工位前持续增长的总队列解释为无风险。
2. 实际到达先服务会改变单车开始、等待和下线次序；R9投车序列和理论投车时间未改变。
3. 本轮不修改等待口径、风险提示、UI、workflow或打包配置。
4. R10专用验证脚本：`ticket_export_app/scripts/verify_v29_round3_r10.py`。
5. R10真实数据验证结果：`ticket_export_app/reports/v2.9_round3_r10_result.json`。

## 第4轮：R1 等待根因正确归位

### 前置结论和范围收紧

1. R10已于 `4602e83 fix shared resource arrival order` 独立提交，R1以该提交为时间链基线。
2. `CAAC`等连续零工时车型仍遵守R2物理车位容量：A1占用下游零工时节点等待时，A2只能停在A1上游的车位，因此A1和A2的原始等待可以不同。R1不修改这些秒数。
3. R1只修正“完工后等待的下游原因岗位”：紧邻下一行为0工时时，继续沿车辆路径查找下一个工时大于0的真实作业岗位。
4. 本轮不删除“根因岗位必须存在原始工时超目标”的现有展示过滤。该过滤与“原始等待/岗位能力瓶颈等待分离”一起留到第5轮，避免首工位外投入等待提前混入瓶颈结果。
5. UI卡片、风险提示和累计阻塞口径本轮不切换。

### 实现内容

1. `core/analysis.py` 新增下一个正工时作业岗位查找逻辑。
2. `block_wait` 依然保留在原车辆、原工位过程记录上，只在分析汇总时将根因追溯到下一个真实作业岗位。
3. 新增 `blocking_root_attributions`，记录来源岗位、紧邻下一节点、最终根因岗位、事件数和等待秒数。
4. 新增未过滤归因摘要：`attributed_blocking_stations`、`attributed_launch_wait_time`和`attributed_post_process_wait_time`，供第5轮分离口径时复核。

### 真实数据归位结果

| 场景 | 跨过0工时节点事件 | 重新归位时间 | 时间链变化 |
|---|---:|---:|---:|
| A276/B306/C566按数量 | 911条 | 32687.0s | 0 |
| A:B:C=2:3:5按比例 | 862条 | 33132.0s | 0 |

主要归位证据：

| 来源岗位 | 被跳过的0工时节点 | 最终根因岗位 | 数量场景 | 比例场景 |
|---|---|---|---:|---:|
| 电检1 | 电检2 | 小跑道 | 229条 / 14460.0s | 246条 / 13015.0s |
| L2++ | 电检1 | 小跑道 | 239条 / 5958.0s | 246条 / 7845.0s |
| ADAS | ARHUD+慢充 | 转毂 | 275条 / 9002.0s | 246条 / 10479.0s |
| 集约 | 空气悬挂+快充 | 四轮定位上、下 | 168条 / 3267.0s | 124条 / 1793.0s |

### 验证边界

1. 最小场景“气囊安装+绝缘 → L2++(0s) → 小跑道”中，20s完工后等待的根因由L2++改为小跑道。
2. 两组完整真实数据在分析前后，每条记录的线别、开始、加工完成、离开、投入等待和完工后等待完全一致。
3. R2车位容量、R9冻结排列和R10实际到达服务顺序不变。
4. 本轮新增验证脚本：`ticket_export_app/scripts/verify_v29_round4_r1.py`。
5. 本轮结果：`ticket_export_app/reports/v2.9_round4_r1_result.json`。
6. 状态：修改和自动验证完成，未提交，等待用户确认。

## 第5轮：原始流转等待与岗位能力瓶颈等待分离

### 实施边界

1. 只在 `core/analysis.py` 新增独立统计字段，不修改排程时间链。
2. 累计实际等待只统计车辆进线后，各工程真实发生的完工后等待；首工位外尚未进线的投入排队单独保留，不混入主指标。
3. 每次等待的工程可接纳上限 = 当前等待工程有效设备能力 × 目标节拍。
4. 每次瓶颈阻塞 = `max(0, 实际等待 - 当前等待工程可接纳上限)`。
5. 等待发生工程表示车辆实际停留位置；下游接收工程严格取该车路径上的紧邻下一节点，包含0工时节点，表示车辆当时无法进入的位置。
6. 下游接收工程只用于解释等待时间链，不等于根因，也不参与超节拍工程命名。
7. 超节拍工程独立按每条作业记录判断：`该车型在本工程加工工时 > 本工程有效设备能力 × 目标节拍`。
8. 0工时节点可以作为等待发生工程或下游接收工程，但其加工工时为0，不会被标记为超节拍工程。
9. 超节拍工时是加工能力风险，瓶颈阻塞是实际等待超过承载上限的结果，两者不得相加。
10. 现有 `total_wait`、累计阻塞卡片和风险提示本轮保持兼容，第6轮再切换UI口径。

### 新增分析字段

- `total_actual_wait`：车辆进线后的累计实际等待。
- `actual_wait_by_station`：按车辆真实停留工程汇总的实际等待。
- `raw_launch_wait`：首工位外投入等待，仅保留调查用途。
- `total_bottleneck_wait`：累计瓶颈阻塞。
- `bottleneck_wait_by_station`：按等待发生工程汇总的瓶颈阻塞。
- `bottleneck_wait_by_downstream_station`：按紧邻下游接收工程汇总瓶颈阻塞，仅作等待链路说明。
- `bottleneck_wait_events`：逐车记录等待工程、紧邻下游接收工程、实际等待、可接纳上限和瓶颈阻塞。
- `capacity_over_takt_processes`：独立列出确实满足“车型工时 > 本工程能力上限”的超节拍工程。
- `capacity_over_takt_total_time`：超节拍工时累计，只描述加工能力风险，不与等待相加。

### 最小口径验证

| 场景 | 实际等待 | 可接纳上限 | 瓶颈阻塞 | 结论 |
|---|---:|---:|---:|---|
| 双设备等待工程，实际停留120s | 120s | 116s | 4s | 仅超出承载上限的4s计入 |
| 单设备等待工程，实际停留70s | 70s | 58s | 12s | 仅超出承载上限的12s计入 |
| ADAS → 0s节点 → ARHUD+慢充，ADAS停留143.5s | 143.5s | 116s | 27.5s | 等待记在ADAS，下游接收/超节拍工程为ARHUD+慢充 |

### 完整真实数据

| 指标 | A276/B306/C566数量场景 | A:B:C=2:3:5比例场景 |
|---|---:|---:|
| 首工位外投入等待（调查项） | 4628463.0s | 6083039.5s |
| 进线后累计实际等待 | 239975.5s | 238387.0s |
| 累计瓶颈阻塞 | 5430.0s | 3768.5s |
| 瓶颈阻塞事件 | 255条 | 187条 |
| 等待发生工程首位 | 电检1：3384.0s | 空气悬挂+快充：2096.0s |
| 紧邻下游接收工程首位 | 电检2：3384.0s | 四轮定位上、下：2096.0s |
| 超节拍工时累计 | 19489.0s | 21514.0s |
| 超节拍工程 | ARHUD+慢充C：27.5s/台；四轮定位B/C：4.5s/台 | ARHUD+慢充C：27.5s/台；四轮定位B/C：4.5s/台 |

### 回归结果

1. 两组真实数据的累计实际等待与所有 `block_wait` 逐条相加完全一致。
2. 新增分析前后排程时间链完全一致。
3. 每条瓶颈阻塞均可由 `max(0, 实际等待 - 等待工程设备能力×58)` 逐条复算。
4. 所有车辆相邻记录均满足“当前工程离开时间 = 紧邻下一工程开始时间”，下游接收工程可由时间链逐项复核。
5. “下游接收工程”和“超节拍工程”已完全分开，工时未超上限的接收工程不会被误标成超节拍工程。
6. R1、R10、R2专用验证继续通过。
7. 第5轮验证脚本：`ticket_export_app/scripts/verify_v29_round5_wait_split.py`。
8. 第5轮结果：`ticket_export_app/reports/v2.9_round5_wait_split_result.json`。
9. 状态：最终口径修改完成，等待完整回归和用户确认后提交。

---

### 第5轮补充调查（新增 2026-06-13）：等待数值可复算，但“最终根因”不可承诺

> 本节为新增内容，不修改上方第5轮原文。由 codex 逐车复核与本次分析合并整理，目的是厘清“等待数值”与“等待原因”的边界，并给出本轮最优输出口径建议。

#### 一、Codex 复核确认（与上方完整真实数据一致）

按数量场景的累计超出可接纳等待 `5430.0s` 可以被三个等待发生工程**完整分解、逐条复算**：

| 等待发生工程 | 下一站（路线信息） | 事件数 | 实际等待合计 | 工程可接纳上限 | 超出可接纳等待 |
|---|---|---:|---:|---:|---:|
| 电检1 | 电检2 | 78 | 12,432.0s | 2×58=116s | 3,384.0s |
| 空气悬挂+快充 | 四轮定位上、下 | 158 | 20,234.0s | 116s | 1,906.0s |
| L2++ | 电检1 | 19 | 2,344.0s | 116s | 140.0s |

三者之和 `3384.0 + 1906.0 + 140.0 = 5430.0s`，与第5轮总额完全吻合。

**修正点**：L2++ 一处早期口径误记为 `1140.0s`，正确值为 `140.0s`（实际等待 2344.0s，可接纳 116s/次，逐次 `max(0, 实际等待−116)` 累计 = 140.0s）。

#### 二、核心分歧：数值可复算，但远端根因不可 100% 反推

逐车追踪后确认，**等待“数值”和等待“原因”是两件事**：

可以 100% 从现有排程数据复算的（6 项）：当前停车工程、下一站、实际等待、工程可接纳上限、超出可接纳等待、每车型每工程的超节拍工时。

**不能** 100% 从现有结果反推的：某一段等待最终由哪一个**远端**工程造成。原因是排程只保存了“等待时长”，**没有在等待产生的瞬间保存“被哪台车、哪个车位、哪个设备挡住”**。

三个真实传导链证明“下一站 ≠ 根因”：

1. **电检1 的 3,384.0s**：Car#18 停电检1 等 193s（超 77s）→ 电检2 被 Car#16 占着 → Car#16 在等末端共用小跑道 → 小跑道先服务更早到达的 Car#15、Car#17。电检1、电检2 **都不超节拍**，真正源头是末端共用设备 + 车辆到达顺序的排队前传。
2. **空气悬挂的 1,906.0s**：Car#17 停空气悬挂等 143.5s（超 27.5s）→ 四轮定位被 Car#15 占着（工时 120.5s，超 4.5s）→ Car#15 又在等 ADAS → ADAS 在等 ARHUD → ARHUD 的 C 车工时 143.5s（超 27.5s）。这段**确含**四轮定位 B/C 与 ARHUD 超节拍的前传影响，但 `1906.0s ≠ 四轮定位 4.5s × 台数`，它是排队超过空气悬挂承载上限后的累计结果。
3. **L2++ 的 140.0s**：Car#596 停 L2++ 等 178.5s（超 62.5s）→ 电检1 被 Car#594 占着 → Car#594 等电检2 → 电检2 被 Car#592 占着 → Car#592 等末端小跑道。同样不代表 L2++、电检1 或电检2 超节拍。

**结论**：超出可接纳等待是“车辆在某工程停留超过其承载上限”的可复算结果，**不能解释成“下一站工程造成”，更不能等同于“下一站超节拍”**。把复杂的多层传导压缩成单一“根因工程”会误导。

#### 三、生产线等待工时与总制品时间的定义（固化口径）

| 术语 | 大白话 | 严格定义 | 数据来源 |
|---|---|---|---|
| 首工位投入等待 | 进线前在线外排队 | 车辆具备投车条件到真正进入首工位的时长 | 仅调查用，不计入主指标 |
| 实际等待（完工后等待） | 完工后卡在原地多久 | 车辆在某工程完工到被下一节点接纳的停留时长 | `离开 − 加工完成` |
| 工程可接纳上限 | 一台车在这工程最多该等多久 | 有效设备数 × 目标节拍（双设备116s、单设备58s） | 设备数 × 58 |
| 超出可接纳等待 | 超过承载能力的那部分 | `max(0, 实际等待 − 工程可接纳上限)` | 逐次计算 |
| 超节拍工程 | 这工程处理这种车本来就慢 | `车型工时 ÷ 有效设备能力 > 目标节拍` | 逐车逐工程 |
| 单车制品时间（贯通时间） | 一台车从进线到下线总共花多久 | `下线时间 − 进线时间` = Σ各工位加工 + Σ各工位完工后等待（+首工位投入等待） | 实际时间链 |
| 全批次总完成时间（makespan） | 整批车最后一台几点下线 | 最后一台目标车辆的实际下线完成时间 | 实际下线记录 |

两条不可违反的口径红线（与既有原则一致）：

1. **总完成时间只用实际下线记录**，不能用任何等待累加推导。
2. **累计等待是多车多工位的并行累加值**，不等于总延误，**不能加进总完成时间**；超节拍工时与超出可接纳等待也**不相加**。

#### 四、本轮等待计算与输出口径的最优调整建议

> 本节保留调查过程中的候选叫法；正式定版术语以第七节“术语定版”为准，其中“超出承载等待”统一改为“超出可接纳等待”。

当前第5轮的数值口径（`超出可接纳等待 = max(0, 实际等待 − 设备数×节拍)`）是正确且可复算的，建议保留。需要调整的是**命名与归属表达**，避免“可复算的数值”被读成“已确认的根因”：

1. **改名去“瓶颈”二字**：把 `total_bottleneck_wait` / `瓶颈阻塞` 对外显示为 **“超出承载等待”**。“瓶颈”二字暗示某个具体工程是根因，但本指标只衡量“本工程停留超出承载上限”，与远端根因无关。内部字段可保留，但 UI 与报表统一用“超出承载等待”。
2. **降级“下游接收工程”归属**：`bottleneck_wait_by_downstream_station`（按下一站汇总）容易让“电检2 = 3384s 责任方”这种误读发生。建议**不再作为独立结果卡片**，仅在逐车明细里以 **“当前停车工程 → 下一站（路线，非根因）”** 出现，并显式标注“路线信息，不代表根因，不代表超节拍”。
3. **超节拍工程独立、且只列真正超的**：继续独立输出 `ARHUD+慢充C 27.5s/台`、`四轮定位B/C 4.5s/台`，与等待指标分区显示，不互相冒充。
4. **模型结果卡片**：把“累计阻塞”改为“**累计超出承载等待 5430.0s**”，并附一行说明：“队列超载指标，不等于某工程超节拍，不可加入总完成时间”。
5. **明确标注“根因不可承诺”**：在第5轮输出里写明——可提供“等待数值 + 等待路径 + 超节拍工程”，**不提供“每段等待的最终远端根因”**；后者需排程改造（见下）。

建议的第5轮正式输出形态：

```
累计实际等待：239,975.5s
累计超出承载等待：5,430.0s
按等待发生工程：电检1 3384.0s / 空气悬挂+快充 1906.0s / L2++ 140.0s …
超节拍工程：ARHUD+慢充C 27.5s/台；四轮定位B/C 4.5s/台
等待路径（仅路由，非根因）：当前停车工程 → 下一站
```

#### 五、关于“最终根因”的后续改造（非本轮）

若业务要求每段等待都能 100% 指出最终远端原因，需要在排程产生等待的瞬间额外记录：被挡住的车号、占用工程、占用车位/设备、阻塞开始与结束时间、阻塞传导链。这属于排程数据采集粒度的扩展，建议列为后续独立轮次（暂记为 R1+/根因事件记录），**本轮不实现**。

#### 六、第5轮提交建议

- 等待数值（实际等待、超出承载等待、超节拍工时）已可复算，可以提交。
- 但需先按第四节调整命名与归属表达，去掉任何“根因工程”承诺，否则交付后易被质疑“这个数到底怎么算、是谁造成的”。
- 采纳本节口径调整后，第5轮可作为“**可复算的等待与超载口径**”提交；“最终根因”明确留待后续轮次。
- 若坚持本轮就要给出 100% 根因，则第5轮**暂不提交**，直接进入根因事件记录改造。

状态：补充调查已于2026-06-13确认采用；正式术语以第七节为准，最终远端根因留待独立事件记录轮次。

#### 七、术语定版（已确认 2026-06-13）

经用户确认，对外统一术语如下，第6轮切换 UI/报表显示时以此为准：

1. **超出可接纳等待** = `max(0, 实际等待 − 可接纳上限)`，其中 `可接纳上限 = 有效设备数 × 目标节拍`。替代早期“瓶颈阻塞”“超出承载等待”等任何叫法（去掉“瓶颈”二字，避免被读成某工程是根因）。
2. **下一工程** = 车辆路径上紧邻的下一个工程（含 0 工时节点）。替代“下一站”“紧邻下游接收工程”。**仅表示路线，不代表根因，也不代表它超节拍。**
3. **可接纳上限**、**实际等待**、**超节拍工程** 术语沿用第5轮原定义不变。
4. 内部分析字段键（`total_bottleneck_wait`、`bottleneck_wait_by_downstream_station`、`downstream_station` 等）本轮**保持英文键名不变**以兼容回归脚本与现有结果；显示层在第6轮按上述中文术语呈现，并在 `core/analysis.py` 字段产出处以注释钉死官方显示名。
5. 对外**不再使用**“根因工程”“阻塞根因”等措辞；最终远端根因需后续“根因事件记录”轮次才能提供。

对照表（内部键 → 对外显示）：

| 内部字段键 | 对外显示术语 |
|---|---|
| `total_bottleneck_wait` | 累计超出可接纳等待 |
| `bottleneck_wait` / `bottleneck_wait_by_station` | 超出可接纳等待（按等待发生工程） |
| `holding_limit` | 可接纳上限（设备数 × 节拍） |
| `actual_wait` / `total_actual_wait` | 实际等待 / 累计实际等待 |
| `downstream_station` / `bottleneck_wait_by_downstream_station` | 下一工程（路线，非根因） |
| `capacity_over_takt_processes` | 超节拍工程 |

#### 八、补充调查确认结论与当前落地边界（确认 2026-06-13）

本次补充调查予以认可，理由与边界如下：

1. 按数量真实场景的 `5430.0s` 已由255次等待事件逐条复算，三个等待发生工程合计完全闭合；L2++正确值为 `140.0s`。
2. 每次超出可接纳等待均可由 `max(0, 实际等待 - 有效设备数×目标节拍)` 复算，不依赖主观判断。
3. 车辆开始、加工完成、离开和下线时间在新增分析前后保持一致，因此该指标是对既有时间链的解释，不是新的排程规则。
4. 三条真实车辆传导链证明“下一工程”不能直接解释为责任工程或最终根因；现有结果没有保存等待瞬间的阻挡对象与完整传导链。
5. 因此认可的正式输出只有：累计实际等待、累计超出可接纳等待、按等待发生工程汇总、下一工程路线信息、独立超节拍工程。
6. 不认可并禁止对外输出：下一工程造成等待、根因工程、阻塞根因已确认等超出现有证据的结论。

当前落地计划：

1. 第5轮仅完成分析字段、术语注释和真实数据回归，不切换UI，不改变排程时间链。
2. 第5轮验证通过并提交后，第6轮仅把模型结果卡片、风险提示和结果说明切换为“累计超出可接纳等待”等定版术语。
3. 第6轮的“下一工程”只允许出现在逐车路线明细，不作为责任工程汇总。
4. 最终远端根因如需100%审计，另开R1+事件记录轮次；该轮需记录阻挡车辆、占用车位/设备、等待起止时间和传导链，不与当前两轮合并。

状态：第5轮调查结论已确认；代码复核与完整回归已通过，按上述边界完成最终落地并进入提交。

最终回归结果：

1. `py_compile`：分析、排程、UI与入口文件全部通过。
2. R2物理车位容量回归：通过。
3. R10共用资源先到先服务回归：通过。
4. R1等待路径归位回归：通过，两组真实数据时间链不变。
5. 第5轮等待拆分回归：通过；按数量场景累计实际等待 `239975.5s`、累计超出可接纳等待 `5430.0s`，按比例场景分别为 `238387.0s`、`3768.5s`。
6. 第5轮未修改UI、排程决策、总完成时间、整体节拍和下线车辆数。
